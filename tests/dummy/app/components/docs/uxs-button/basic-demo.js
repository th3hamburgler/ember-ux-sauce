import Component from '@ember/component';

export default Component.extend({
  disabled: false,
  inline: false,
  loading: false,
  loadingText: "Loading...",
  naked: false,
  text: 'DEMO BUTTON',
});