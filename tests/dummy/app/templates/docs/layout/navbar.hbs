<h1>Navbar</h1>

<p>The navbar component is used to render the main nav bar of your application.</p>

<h2>Usage</h2>

<p>The example below shows common usage of the nav bar with an embeded {{link-to "navigation list" "docs.layout"}}.</p>

{{#docs-demo as |demo|}}

  {{#demo.example name="navbar"}}

    {{#uxs-navbar title=title fixed=fixed as |bar|}}
      {{#bar.menu as |nav|}}
        {{nav.item "Home"}}
        {{nav.item "Account"}}
        {{nav.item "Logout"}}
      {{/bar.menu}}
    {{/uxs-navbar}}

  {{/demo.example}}

  {{demo.snippet "navbar.hbs"}}

{{/docs-demo}}


<h2>Properties</h2>

<p>Now explore the various properties available in this component:</p>

<table class="docs-args-table">
  <tr>
    <td>title</td>
    <td>{{input placeholder="App Title" value=title}}</td>
    <td><em>String</em></td>
  </tr>
  <tr>
    <td>fixed</td>
    <td>{{docs-toggle value=fixed}}</td>
    <td><em>Boolean</em></td>
  </tr>
</table>

<h2>Styles Usage</h2>

<p>You can customise the look of your navbar with the style attribute.</p>

{{#docs-demo as |demo|}}

  {{#demo.example name="navbar-styles"}}

    {{#each styles as |style|}}

      {{#uxs-navbar
        style=style
        title=(capitalize (concat style " Navbar"))
        as |bar|
      }}

        {{#bar.desktopNav as |nav|}}
          {{#nav.menu detail=true as |menu|}}
            {{menu.link "Home"}}
            {{menu.item "Account"}}
            {{menu.item "Logout"}}
          {{/nav.menu}}
        {{/bar.desktopNav}}

      {{/uxs-navbar}}

    {{/each}}

  {{/demo.example}}

  {{demo.snippet "navbar-styles.hbs"}}

{{/docs-demo}}
