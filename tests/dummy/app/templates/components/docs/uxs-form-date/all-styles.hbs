{{#docs-demo as |demo|}}

  {{#demo.example name='form-date-styles.hbs'}}

    {{uxs-form-date placeholder="DEFAULT" date=(readonly date) onChange=(action "setDate") class="mb"}}
    {{uxs-form-date placeholder="PRIMARY" style="primary" date=(readonly date) onChange=(action "setDate") class="mb"}}
    {{uxs-form-date placeholder="ACCENT" style="accent" date=(readonly date) onChange=(action "setDate") class="mb"}}
    {{uxs-form-date placeholder="WARNING" style="warning" date=(readonly date) onChange=(action "setDate") class="mb"}}
    {{uxs-form-date placeholder="ERROR" style="error" date=(readonly date) onChange=(action "setDate") class="mb"}}
    {{uxs-form-date placeholder="DARK" style="dark" date=(readonly date) onChange=(action "setDate") class="mb"}}
    {{uxs-form-date placeholder="GREY" style="grey" date=(readonly date) onChange=(action "setDate") class="mb"}}
    {{uxs-form-date placeholder="MID" style="mid" date=(readonly date) onChange=(action "setDate") class="mb"}}
    {{uxs-form-date placeholder="LIGHT" style="light" date=(readonly date) onChange=(action "setDate") class="mb"}}
    {{uxs-form-date placeholder="WHITE" style="white" date=(readonly date) onChange=(action "setDate") class="mb"}}

  {{/demo.example}}

  {{demo.snippet 'form-date-styles.hbs'}}
{{/docs-demo}}
