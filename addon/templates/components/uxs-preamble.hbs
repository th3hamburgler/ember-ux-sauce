{{yield
  (
    hash
    body=(
      component
      "uxs-div"
      class=(bem b "body" m)
      testSelectorName=(concat b "-body")
    )
    icon=(
      component
      "uxs-icon"
      base=(concat b "__icon")
      class=(bem b "icon" m)
    )
    content=(
      component
      "uxs-content"
      class=(bem b "content" m)
      testSelectorName=(concat b "-content")
    )
  )
}}
