{{yield (hash
  control=(component "uxs-form-control" model=model disabled=disabled name=name)
  button=(component "uxs-button" disabled=disabled name=name)
  submit=(component "uxs-button" disabled=disabled name=name type="submit")
  link=(component "uxs-button-link" disabled=disabled name=name)
  row=(component "uxs-form-control-row" model=model name=name disabled=disabled)
)}}
