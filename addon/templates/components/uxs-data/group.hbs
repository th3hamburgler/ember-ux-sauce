{{yield
  (
    hash
    label=(
      component
      "uxs-data/label"
      display=display
      padded=padded
      inset=inset
      grouped=true
      name=name
    )
    value=(
      component
      "uxs-data/value"
      display=display
      padded=padded
      inset=inset
      grouped=true
      name=name
    )
    tip=(
      component
      "uxs-data/tip"
      display=display
      padded=padded
      inset=inset
      grouped=true
      name=name
    )
  )
}}
