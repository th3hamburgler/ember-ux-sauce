{{yield
  (hash
    label=(
      component "uxs-form/label"
      disabled=@disabled
      style=@style
    )
    input=(
      component "uxs-form/input"
      disabled=@disabled
      style=@style
    )
    tip=(
      component "uxs-form/tip"
      disabled=@disabled
      style=@style
    )
  )
}}
